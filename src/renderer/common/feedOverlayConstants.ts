import { FEED_OVERLAY_LOADING_PAYLOAD } from '@shared/feedOverlaySentinel'

/**
 * Passed as `payloadJson` to `set-main-area-feed-mode`; same as main `@shared/feedOverlaySentinel`.
 */
export const MAIN_FEED_OVERLAY_LOADING_PAYLOAD = FEED_OVERLAY_LOADING_PAYLOAD
